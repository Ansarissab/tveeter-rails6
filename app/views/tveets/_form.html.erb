<%= form_with(model: tveet, id: dom_id(tveet)) do |form| %>
  <% if tveet.errors.any? %>
    <div id="error_explanation">
      <h6><%= pluralize(tveet.errors.count, "error") %> prohibited this tveet from being saved:</h6>

      <ul>
        <% tveet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_area :body, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit tveet.persisted? ? 'Update' : 'Create', class: 'btn btn-success' %>
  </div>
<% end %>

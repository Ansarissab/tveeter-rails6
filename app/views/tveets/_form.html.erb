<%= form_with(model: tveet) do |form| %>
  <% if tveet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tveet.errors.count, "error") %> prohibited this tveet from being saved:</h2>

      <ul>
        <% tveet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :likes_count %>
    <%= form.number_field :likes_count %>
  </div>

  <div class="field">
    <%= form.label :retweets_count %>
    <%= form.number_field :retweets_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
